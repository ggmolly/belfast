bind_address = "0.0.0.0"
# Gateway port (defaults to 80)
port = 80

[[servers]]
# Server list entries (used by gateway).
id = 1
ip = "127.0.0.1"
port = 7000
api_port = 2289
# When true, mark this server online without calling /api/v1/server/status.
# assert_online = true
# proxy_ip = "127.0.0.1"
# proxy_port = 7001
