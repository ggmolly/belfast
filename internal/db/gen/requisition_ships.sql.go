// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: requisition_ships.sql

package gen

import (
	"context"
)

const getRandomRequisitionShipByRarity = `-- name: GetRandomRequisitionShipByRarity :one
SELECT s.template_id, s.name, s.english_name, s.rarity_id, s.star, s.type, s.nationality, s.build_time, s.pool_id
FROM ships s
JOIN requisition_ships rs ON rs.ship_id = s.template_id
WHERE s.rarity_id = $1
ORDER BY RANDOM()
LIMIT 1
`

func (q *Queries) GetRandomRequisitionShipByRarity(ctx context.Context, rarityID int64) (Ship, error) {
	row := q.db.QueryRow(ctx, getRandomRequisitionShipByRarity, rarityID)
	var i Ship
	err := row.Scan(
		&i.TemplateID,
		&i.Name,
		&i.EnglishName,
		&i.RarityID,
		&i.Star,
		&i.Type,
		&i.Nationality,
		&i.BuildTime,
		&i.PoolID,
	)
	return i, err
}

const listRequisitionShipIDs = `-- name: ListRequisitionShipIDs :many

SELECT ship_id
FROM requisition_ships
ORDER BY ship_id ASC
`

// Requisition ship queries
func (q *Queries) ListRequisitionShipIDs(ctx context.Context) ([]int64, error) {
	rows, err := q.db.Query(ctx, listRequisitionShipIDs)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []int64
	for rows.Next() {
		var ship_id int64
		if err := rows.Scan(&ship_id); err != nil {
			return nil, err
		}
		items = append(items, ship_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
