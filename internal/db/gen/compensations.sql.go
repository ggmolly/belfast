// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: compensations.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createCompensation = `-- name: CreateCompensation :one

INSERT INTO compensations (
  commander_id,
  title,
  text,
  send_time,
  expires_at,
  attach_flag
) VALUES (
  $1, $2, $3, $4, $5, $6
)
RETURNING id, send_time, created_at
`

type CreateCompensationParams struct {
	CommanderID int64
	Title       string
	Text        string
	SendTime    pgtype.Timestamptz
	ExpiresAt   pgtype.Timestamptz
	AttachFlag  bool
}

type CreateCompensationRow struct {
	ID        int64
	SendTime  pgtype.Timestamptz
	CreatedAt pgtype.Timestamptz
}

// Compensation queries
func (q *Queries) CreateCompensation(ctx context.Context, arg CreateCompensationParams) (CreateCompensationRow, error) {
	row := q.db.QueryRow(ctx, createCompensation,
		arg.CommanderID,
		arg.Title,
		arg.Text,
		arg.SendTime,
		arg.ExpiresAt,
		arg.AttachFlag,
	)
	var i CreateCompensationRow
	err := row.Scan(&i.ID, &i.SendTime, &i.CreatedAt)
	return i, err
}

const createCompensationAttachment = `-- name: CreateCompensationAttachment :exec
INSERT INTO compensation_attachments (
  compensation_id,
  type,
  item_id,
  quantity
) VALUES (
  $1, $2, $3, $4
)
`

type CreateCompensationAttachmentParams struct {
	CompensationID int64
	Type           int64
	ItemID         int64
	Quantity       int64
}

func (q *Queries) CreateCompensationAttachment(ctx context.Context, arg CreateCompensationAttachmentParams) error {
	_, err := q.db.Exec(ctx, createCompensationAttachment,
		arg.CompensationID,
		arg.Type,
		arg.ItemID,
		arg.Quantity,
	)
	return err
}

const deleteCompensation = `-- name: DeleteCompensation :exec
DELETE FROM compensations
WHERE id = $1
`

func (q *Queries) DeleteCompensation(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, deleteCompensation, id)
	return err
}

const deleteCompensationAttachmentsByCompensationID = `-- name: DeleteCompensationAttachmentsByCompensationID :exec
DELETE FROM compensation_attachments
WHERE compensation_id = $1
`

func (q *Queries) DeleteCompensationAttachmentsByCompensationID(ctx context.Context, compensationID int64) error {
	_, err := q.db.Exec(ctx, deleteCompensationAttachmentsByCompensationID, compensationID)
	return err
}

const getCompensation = `-- name: GetCompensation :one
SELECT id,
       commander_id,
       title,
       text,
       send_time,
       expires_at,
       attach_flag,
       created_at
FROM compensations
WHERE id = $1
`

func (q *Queries) GetCompensation(ctx context.Context, id int64) (Compensation, error) {
	row := q.db.QueryRow(ctx, getCompensation, id)
	var i Compensation
	err := row.Scan(
		&i.ID,
		&i.CommanderID,
		&i.Title,
		&i.Text,
		&i.SendTime,
		&i.ExpiresAt,
		&i.AttachFlag,
		&i.CreatedAt,
	)
	return i, err
}

const listCompensationAttachmentsByCompensationIDs = `-- name: ListCompensationAttachmentsByCompensationIDs :many
SELECT id,
       compensation_id,
       type,
       item_id,
       quantity
FROM compensation_attachments
WHERE compensation_id = ANY($1::bigint[])
ORDER BY id ASC
`

func (q *Queries) ListCompensationAttachmentsByCompensationIDs(ctx context.Context, dollar_1 []int64) ([]CompensationAttachment, error) {
	rows, err := q.db.Query(ctx, listCompensationAttachmentsByCompensationIDs, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CompensationAttachment
	for rows.Next() {
		var i CompensationAttachment
		if err := rows.Scan(
			&i.ID,
			&i.CompensationID,
			&i.Type,
			&i.ItemID,
			&i.Quantity,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listCompensationsByCommander = `-- name: ListCompensationsByCommander :many
SELECT id,
       commander_id,
       title,
       text,
       send_time,
       expires_at,
       attach_flag,
       created_at
FROM compensations
WHERE commander_id = $1
ORDER BY id ASC
`

func (q *Queries) ListCompensationsByCommander(ctx context.Context, commanderID int64) ([]Compensation, error) {
	rows, err := q.db.Query(ctx, listCompensationsByCommander, commanderID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Compensation
	for rows.Next() {
		var i Compensation
		if err := rows.Scan(
			&i.ID,
			&i.CommanderID,
			&i.Title,
			&i.Text,
			&i.SendTime,
			&i.ExpiresAt,
			&i.AttachFlag,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateCompensation = `-- name: UpdateCompensation :exec
UPDATE compensations
SET title = $2,
    text = $3,
    send_time = $4,
    expires_at = $5,
    attach_flag = $6
WHERE id = $1
`

type UpdateCompensationParams struct {
	ID         int64
	Title      string
	Text       string
	SendTime   pgtype.Timestamptz
	ExpiresAt  pgtype.Timestamptz
	AttachFlag bool
}

func (q *Queries) UpdateCompensation(ctx context.Context, arg UpdateCompensationParams) error {
	_, err := q.db.Exec(ctx, updateCompensation,
		arg.ID,
		arg.Title,
		arg.Text,
		arg.SendTime,
		arg.ExpiresAt,
		arg.AttachFlag,
	)
	return err
}
